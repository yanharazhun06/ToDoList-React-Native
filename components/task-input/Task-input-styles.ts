import { StyleSheet } from "react-native";

export const taskInputStyles = StyleSheet.create({
    input: {
    width: "90%",
    height: 50,
    padding: 10,
    fontSize: 18,
    borderBottomWidth: 1,
    borderColor: "#fff",
    alignSelf:"center",
    color: "#fff"
  },
});