import { StyleSheet } from "react-native";

export const taskInputStyles = StyleSheet.create({
    input: {
    width: 200,
    height: 50,
    padding: 10,
    fontSize: 18,
    borderBottomWidth: 1,
    borderColor: "#57C785",
  },
});